@model IEnumerable<WebApplication.Models.Payment>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Платежи</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Listener.FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Listener.Surname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Listener.Patronymic)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Purpose)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Listener.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Listener.Surname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Listener.Patronymic)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Purpose.PurposeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
